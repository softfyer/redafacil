rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Regra SUPER permissiva para testes
    match /essays/{studentId}/{allPaths=**} {
      allow read, write: if true; // Permite TUDO
    }
  }
}